# Dev / test dependencies (includes runtime). Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
httpx==0.27.2          # used by fastapi's TestClient in the suite
