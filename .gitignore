__pycache__/
*.pyc
.pytest_cache/
*.db
*.db-wal
*.db-shm
config.yaml
.env
_local/
nas_check.sh
tools/
# Personal library path dumps (parser validation fixtures) — never publish
tv.txt
movies.txt
# Local Claude Code tooling config (machine-specific)
.claude/
# Personal-deployment runtime dirs (never publish quarantined media or logs)
quarantine/
logs/
